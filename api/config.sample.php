<?php
/**
 * Образец настроек приёма заявок.
 *
 * Скопировать в api/config.php и вписать значения. Сам config.php в репозиторий
 * не попадает (он в .gitignore) и на хостинг уезжает обычной выкладкой.
 *
 * Ключ входящего вебхука даёт полный доступ к CRM. Он живёт только здесь,
 * на сервере: ни в разметке, ни в app.js, ни в git.
 */

return array(

  /**
   * Входящий вебхук Битрикс24 с правами на CRM.
   * Битрикс24 → Разработчикам → Другое → Входящий вебхук.
   * Вид: https://ваш-портал.bitrix24.ru/rest/1/xxxxxxxxxxxxxxxx/
   * Слэш на конце обязателен.
   */
  'b24_webhook' => '',

  /**
   * Ответственный за сделки с сайта — ID сотрудника в Битриксе24.
   * 1 — обычно администратор портала. Посмотреть: профиль сотрудника,
   * число в адресе страницы /company/personal/user/123/
   */
  'b24_assigned_by' => 1,

  /**
   * Воронка, в которую падают сделки с сайта — ID категории сделок.
   * На портале заказчика: 11 — «ПРОДАЖИ», 0 — «Склад лидов».
   * Без явного указания Битрикс кладёт сделку в воронку по умолчанию,
   * а по умолчанию там стоит «Склад лидов» — и заявки теряются не в том месте.
   * Список воронок: crm.category.list с entityTypeId=2.
   */
  'b24_deal_category' => 11,

  /**
   * Поле компании, куда ложится приложенная к заявке карточка клиента.
   * На портале заказчика это UF_CRM_1774530589618 — «Карточка клиента + документы»,
   * множественное поле типа «файл».
   * Пустая строка отключает запись в компанию: тогда карточка идёт только в сделку.
   */
  'b24_company_card_field' => 'UF_CRM_1774530589618',

  /**
   * Создавать ли счёт сразу после сделки.
   * true  — счёт появляется в CRM автоматически, отправку письма клиенту
   *         настраивает робот в Битриксе на стадии счёта;
   * false — менеджер выставляет счёт из сделки руками, подтвердив наличие.
   */
  'b24_create_invoice' => false,

  /**
   * Куда писать, если Битрикс24 не ответил. Заявка при этом всё равно
   * сохраняется в api/orders/ — потерять её нельзя ни при каких условиях.
   */
  'fallback_email' => 'info@stomtech.pro',

  /** Не больше стольких заявок с одного адреса в час. */
  'rate_limit_per_hour' => 10,
);
